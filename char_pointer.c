#include <stdio.h>
/**
 * main - still practicing pointers
 *
 * Return: always 0
 */
int main(void)
{
	char *str = "Hello world!";/*Declare a character
	pointer and initialize it to point to the string
	"Hello world!"*/
	printf("%s\n", str);/*Output: Hello world*/
}